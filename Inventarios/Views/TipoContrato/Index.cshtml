@model IEnumerable<Inventarios.Models.TipoContrato>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Agregar nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdTipoContrato }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IdTipoContrato }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdTipoContrato })
        </td>
    </tr>
}

</table>
