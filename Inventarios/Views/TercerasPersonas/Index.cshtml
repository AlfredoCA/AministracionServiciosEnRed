@model IEnumerable<Inventarios.Models.TercerasPersonas>

@{
    ViewBag.Title = "Terceras personas";
}

<h2>Terceras Personas</h2>

<p>
    @Html.ActionLink("Agregar nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoCompania.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoCompania.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdTerceraPersona }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IdTerceraPersona }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdTerceraPersona })
        </td>
    </tr>
}

</table>
